@model List<ResultBrandDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="container-fluid">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Araba Marka Listesi</h4>
            <div class="table-responsive">
                <table class="table mb-0">
                    <thead>
                        <tr>
                            <th>Marka</th>
                            <th>Sil</th>
                            <th>Düzenle</th>
                            <th>Araçlar</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.Name</td>
                                <td><a href="/Admin/Brand/RemoveBrand/@item.BrandID" class="btn btn-outline-danger">Sil</a></td>
                                <td><a href="/Admin/Brand/UpdateBrand/@item.BrandID" class="btn btn-outline-dark">Düzenle</a></td>
                                <td><a href="/Admin/Brand/UpdateBrand/@item.BrandID" class="btn btn-outline-success">Araçlar</a></td>
                            </tr>
                        }
                    </tbody>
                </table>
                <a href="/Admin/Brand/CreateBrand" class="btn btn-outline-secondary">Yeni Ekle</a>
            </div>
        </div>
    </div>
</div>


